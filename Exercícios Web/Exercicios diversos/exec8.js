function primeiroEUltimo(array) {
    let indicePrimeiroElemento = 0
    let indiceUltimoElemento = array.length - 1
    
}
const novoArray = array.filter(elemento =>[indicePrimeiroElemento, indiceUltimoElemento])
    console.log(novoArray)

primeiroEUltimo([1, 2, 'olá', 12, 'zé'])
